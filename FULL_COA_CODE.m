

function [ output_args ] = FULL_COA_CODE( input_args )

% For each FUP.

% Calculate best route.

% For all co-ordinates on route.

% If co-ordinate is an event.

% Check if it a node (enemy change). I.e. entering En FOV (check use of
% blinds) (set 'event' checkbit), reaching FUP (set 'FUP' flag).

% If it is not, continue.

% If it is, then save node information.

% If the event checkbit is '1', then calculate new FUP and route

end

